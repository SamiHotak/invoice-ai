# InvoiceAI - runtime requirements (pinned).
# These exact versions were tested together on Google Colab (Python 3.13, T4 GPU).
# For tests and evaluation, install requirements-dev.txt instead (it includes this file).
#
# PyTorch: Colab already has torch 2.11.0 with CUDA 13. The PyPI wheels of
# torch 2.11 use CUDA 13 too, which needs an NVIDIA driver >= 580. For an older
# driver, install torch first from https://pytorch.org with a matching CUDA
# version (see the Dockerfile for an example).
torch==2.11.0
torchvision==0.26.0

# Vision-language model (Qwen2.5-VL). transformers 5 needs huggingface-hub 1.x,
# and gradio 6 too; we stay on transformers 4 + gradio 5, which work together.
transformers==4.57.6
accelerate==1.15.0
qwen-vl-utils==0.0.14
huggingface-hub==0.36.2

# OCR: PaddleOCR (PP-OCR) models via RapidOCR + ONNX Runtime, on CPU.
# (The paddleocr package itself crashes in Colab and clashes with PyTorch.)
rapidocr==3.9.2
onnxruntime==1.30.0
opencv-python>=4.10,<5

# PDF, matching, images, validation
pymupdf==1.28.2
rapidfuzz==3.14.6
numpy==2.1.3
pillow==11.3.0
pydantic==2.12.3

# API, web interface and export
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32
gradio==5.50.0
pandas==2.2.3
openpyxl==3.1.5
