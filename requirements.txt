# InvoiceAI - requirements (Day 4)
# Torch is preinstalled on Google Colab. For local use, install torch
# first from https://pytorch.org (pick the right CUDA version).
torch>=2.2
torchvision>=0.17

# Vision-language model (Qwen2.5-VL needs transformers >= 4.49)
transformers>=4.51,<5
accelerate>=0.30
qwen-vl-utils>=0.0.8

# OCR: PaddleOCR (PP-OCR) models via RapidOCR + ONNX Runtime, on CPU.
# (The paddleocr package itself crashes in Colab and clashes with PyTorch.)
rapidocr>=3.0,<4
onnxruntime>=1.18

# PDF, matching, images, validation
pymupdf>=1.24
rapidfuzz>=3.0
numpy>=1.24
pillow>=10.1
pydantic>=2.6,<3

# Evaluation (SROIE dataset from Hugging Face)
datasets>=2.18

# API and export
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
pandas>=2.0
openpyxl>=3.1

# Tests
pytest>=8.0
httpx>=0.27

# Added later (do not install yet):
# Day 5: gradio
